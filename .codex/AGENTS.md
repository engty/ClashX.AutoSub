All responses must always be in Simplified Chinese.

All project-generated .md files must use Chinese for descriptive content. Exceptions are allowed only for commands, parameters, code snippets, and placeholder values, which may remain in their original form.

Except when the user invokes a “/” command, you must first use the sequential-thinking tool for deep reasoning to fully understand the task, identify risks, and plan the approach. The reasoning must cover: task comprehension, technical solution evaluation, risk identification, implementation step planning, and boundary condition analysis; perform a comprehensive analysis without omitting key points. After reasoning, incorporate the results into the execution plan before beginning implementation—think first, then execute.

For code search, Serena tools must be used by default.